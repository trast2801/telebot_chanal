# Telegram Channel Message Forwarder Bot

Telegram бот для автоматического копирования сообщений из нескольких каналов в один целевой чат/канал с продвинутой фильтрацией и проверкой на дубликаты.

## ✨ Особенности

- 🔄 **Автоматическое копирование** сообщений из нескольких исходных каналов
- 🚫 **Фильтрация контента** по черному списку ключевых слов и регулярных выражений
- 🔍 **Проверка на дубликаты** с использованием алгоритма Жаккара (80% схожести)
- ⏱️ **Трекер задержек** для мониторинга производительности
- 📝 **Подробное логирование** в файл и консоль
- 🎯 **Поддержка медиа** - фото, видео, документы, веб-страницы
- 🔒 **Экранирование Markdown** для корректного отображения сообщений

## 🛠 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/trast2801/telebot_chanal.git
cd telebot_chanal
2. Установка зависимостей
bash
pip install -r requirements.txt
3. Настройка конфигурации
Отредактируйте файл config.py:
2. Установка зависимостей
bash
pip install -r requirements.txt
3. Настройка конфигурации
Отредактируйте файл config.py:

python
# Настройки Telegram API
API_ID = 12345678                    # Ваш API ID из my.telegram.org
API_HASH = 'your_api_hash_here'      # Ваш API Hash из my.telegram.org

# ID целевого чата для отправки сообщений
TARGET_CHAT = -1001234567890         # ID вашего целевого канала/чата

# Список исходных каналов для мониторинга
SOURCE_CHANNELS = [
    -1001111111111,                  # Первый канал
    -1002222222222,                  # Второй канал
    # Добавьте нужные каналы...
]

# Дополнительные настройки в config.py...
4. Получение Telegram API credentials
Перейдите на my.telegram.org

Войдите в свой аккаунт

Перейдите в раздел "API Development Tools"

Создайте новое приложение и получите API_ID и API_HASH

🚀 Запуск
bash
python main.py
При первом запуске потребуется ввести номер телефона и код подтверждения.

⚙️ Конфигурация
Основные настройки в config.py:
python
# Настройки фильтрации
BLACKLIST_KEYWORDS = ['#крипто', '#сша', 'кэшбэк']
BLACKLIST_PATTERNS = [r'спам', r'реклам', r'buy now']

# Настройки проверки дубликатов
SIMILARITY_THRESHOLD = 0.8           # Порог схожести (80%)
DUPLICATE_WINDOW_HOURS = 1           # Время проверки дубликатов

# Настройки логирования
LOG_FILE = 'telegram_bot.log'
LOG_LEVEL = 'INFO'                   # DEBUG, INFO, WARNING, ERROR
Как получить ID канала/чата:
Для публичных каналов: используйте ссылку формата @username

Для приватных каналов/чатов:

Добавьте бота в канал/чат

Отправьте любое сообщение

Бот автоматически определит ID и выведет его в лог

📊 Функциональность
Фильтрация сообщений
Ключевые слова: простой список запрещенных слов

Регулярные выражения: сложные паттерны для фильтрации

Чувствительность к регистру: настраиваемая опция

Проверка дубликатов
Алгоритм Жаккара: вычисление схожести текстов

Нормализация: удаление хештегов, URL, специальных символов

Временное окно: проверка только за последний час

Обработка медиа
Фотографии и видео: копирование с сохранением качества

Документы: передача файлов

Веб-страницы: обработка ссылок с превью

Fallback механизм: обработка ошибок при отправке

📁 Структура проекта
text
telebot_chanal/
├── main.py                 # Основной файл бота
├── config.py              # Конфигурация
├── logger.py              # Система логирования
├── duplicate_checker.py   # Проверка дубликатов
├── message_filter.py      # Фильтрация сообщений
├── delay_tracker.py       # Трекер задержек
├── message_handler.py     # Обработчик сообщений
├── utils.py               # Вспомогательные функции
├── requirements.txt       # Зависимости
└── README.md             # Документация
🐛 Решение проблем
Ошибка авторизации
bash
❌ API_ID должен быть числом, а не строкой!
Убедитесь, что API_ID указан как число без кавычек.

Бот не видит сообщения
Убедитесь, что бот добавлен в исходные каналы

Проверьте права доступа бота в каналах

Убедитесь, что бот является администратором в целевом чате

Проблемы с кодировкой логов
Для Windows добавьте в logger.py:

python
file_handler = logging.FileHandler(log_file, encoding='utf-8')
📝 Логирование
Логи сохраняются в файл telegram_bot.log с форматом:

text
2024-01-15 14:30:25 - INFO - Сообщение из 'Channel Name': Текст сообщения...
2024-01-15 14:30:26 - WARNING - Дубликат из 'Channel Name' (схожесть: 85.0%): нормализованный текст...
